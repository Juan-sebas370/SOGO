server.port=8080
spring.jackson.serialization.indent_output=true

# Mail placeholders (configure for production)
spring.mail.host=
spring.mail.port=
spring.mail.username=
spring.mail.password=

# JWT secret (set in env or here for development only)
app.jwt.secret=change-me-dev-secret-change-me-dev-secret-123456
app.jwt.expiration-minutes=60

# JPA / H2 dev datasource (for prototyping)
spring.datasource.url=jdbc:h2:mem:sogodb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

# For MySQL in production replace the above with:
# spring.datasource.url=jdbc:mysql://localhost:3306/sogodb
# spring.datasource.username=root
# spring.datasource.password=SECRET

# Frontend URL, used to redirect back after a social login
app.frontend-url=http://localhost:4200

# --- OAuth2 social login (optional) ---
# Set these via env vars (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, AZURE_CLIENT_ID, AZURE_CLIENT_SECRET)
# or paste them here for local development only (never commit real secrets).
# Deliberately NOT using the standard spring.security.oauth2.client.registration.* keys: Spring Boot
# validates those eagerly at startup and refuses to boot the whole app if a client-id is blank.
# OAuth2ClientConfig instead builds the registrations itself and simply skips any provider whose
# id/secret aren't set, so the app (and plain email/password login) still starts without them.
# Google: create credentials at https://console.cloud.google.com/apis/credentials
#   Authorized redirect URI: http://localhost:8080/login/oauth2/code/google
app.oauth2.google.client-id=${GOOGLE_CLIENT_ID:}
app.oauth2.google.client-secret=${GOOGLE_CLIENT_SECRET:}

# Microsoft (Entra ID / Azure AD): register an app at https://entra.microsoft.com
#   Authorized redirect URI: http://localhost:8080/login/oauth2/code/azure
#   Supported account types: any (multi-tenant + personal)
app.oauth2.azure.client-id=${AZURE_CLIENT_ID:}
app.oauth2.azure.client-secret=${AZURE_CLIENT_SECRET:}
